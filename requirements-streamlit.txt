# Local install for the Streamlit dashboard (kept for the documented command):
#   .venv/Scripts/python.exe -m pip install -r requirements-streamlit.txt
# All runtime deps now live in requirements.txt (the file Streamlit Cloud reads),
# so this just points at it to avoid drift.
-r requirements.txt
