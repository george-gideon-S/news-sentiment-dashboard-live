# Dependencies for the News Sentiment Dashboard (Streamlit app — app.py).
# Streamlit Community Cloud installs from this file automatically, so it must
# list everything the app imports at runtime.
#
# The static Vercel deploy ignores this file (see .vercelignore). The optional
# serverless function api/refresh.py needs only the last two (feedparser +
# vaderSentiment).
streamlit>=1.33
plotly>=5.18
pandas>=2.0
feedparser>=6.0.11
vaderSentiment>=3.3.2
